# storage

Storage Demo

# This Section

- add [plugin](https://pub.dev/)
    - shared_preferences
    - provider
- use command line
  ```bash
  flutter pub add shared_preferences
  flutter pub add provider
  flutter pub get
  ```